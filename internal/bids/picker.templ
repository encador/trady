package bids

import "github.com/encador/trady/internal/models"

templ Picker(bids models.Bids) {
	<div id="picker" data-ref:picker data-on:click="if (!$over-window) {$picker.style.display='none'}">
		@PickerCSS()
		<div
			id="picker-window"
			data-on:mouseenter="console.log('enter'); $over-window=true"
			data-on:mouseleave="console.log('leave'); $over-window=false"
		>
			<h1>WOW</h1>
		</div>
	</div>
}

templ PickerCSS() {
	<style>
		#picker {
			position: absolute;
			background-color: #00000050;
			display: flex;
			justify-content: center;
			align-items: center;
			inset: 0;
			cursor: pointer;
		}
		#picker-window {
			position: absolute;
			width: 900px;
			height: 500px;
			background-color: var(--dark-2);
			color: var(--white-3);
			cursor: default;
		}
	</style>
}
